// @ts-check
import { defineConfig } from 'astro/config';

// `site` and `base` get set in build-order step 7 (GitHub Pages deploy) —
// project pages need base: '/<repo>/' unless a custom domain is added.
export default defineConfig({});
